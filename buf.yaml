version: v1beta1
name: buf.build/weaveworks/weave-gitops-enterprise
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
build:
  excludes:
    - cmd
  roots:
    - ./
lint:
  # We use .protolint.yaml to lint
