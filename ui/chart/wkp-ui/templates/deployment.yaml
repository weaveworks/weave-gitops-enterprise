{{- if .Values.enabled }}
{{- $configMapName := .Values.config.homepage.clusterComponents.configMap.name -}}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "wkp-ui.fullname" . }}-server
  labels:
    name: {{ include "wkp-ui.fullname" . }}-server
spec:
  selector:
    matchLabels:
      name: {{ include "wkp-ui.fullname" . }}-server
  replicas: 1
  revisionHistoryLimit: 2
  minReadySeconds: 30
  strategy:
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        name: {{ include "wkp-ui.fullname" . }}-server
      annotations:
        # TODO(fbarl): Consider https://github.com/weaveworks/wkp-ui/issues/165 instead.
        prometheus.io.scrape: "false"
    spec:
      volumes:
      - name: ui-config-volume
        configMap:
          name: {{ include "wkp-ui.fullname" . }}-config
      {{- if $configMapName }}
      - name: cluster-components-config-volume
        configMap:
          name: {{ $configMapName }}
      {{- end }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
      - name: {{ . }}
      {{- end}}
      containers:
      - name: {{ include "wkp-ui.fullname" . }}-server
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: SSH_KNOWN_HOSTS
          value: /etc/ssh/ssh_known_hosts
        args:
        - --git-url={{ .Values.config.gitRepo.url }}
        - --git-poll-interval=15s
        - --html-root-path={{ .Values.server.htmlRootPath }}
        ports:
        - containerPort: {{ .Values.service.port }}
        volumeMounts:
        - name: ui-config-volume
          mountPath: /html/wkp-ui/ui-config/
        {{- if $configMapName }}
        - name: cluster-components-config-volume
          mountPath: /html/wkp-ui/cluster-components-config/
        {{- end }}

{{- end }}
