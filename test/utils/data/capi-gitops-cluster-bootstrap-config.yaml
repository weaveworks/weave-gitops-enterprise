apiVersion: capi.weave.works/v1alpha1
kind: ClusterBootstrapConfig
metadata:
  name: capi-gitops
  namespace: default
spec:
  clusterSelector:
    matchLabels:
      weave.works/capi: bootstrap
  jobTemplate:
    generateName: "run-gitops-{{ .ObjectMeta.Name }}"
    spec:
      containers:
        - image: ghcr.io/weaveworks/wego-app:v0.6.0
          name: gitops-install
          resources: {}
          volumeMounts:
            - name: kubeconfig
              mountPath: "/etc/gitops"
              readOnly: true
          args:
            [
              "install",
              "--override-in-cluster",
              "--config-repo",
              "$(GITOPS_REPO)",
            ]
          env:
            - name: KUBECONFIG
              value: "/etc/gitops/value"
            - name: GITOPS_REPO
              value: "{{GITOPS_REPO}}"
            - name: {{GIT_PROVIDER_TOKEN}}
              valueFrom:
                secretKeyRef:
                  name: my-pat
                  key: {{GIT_PROVIDER_TOKEN}}          
      restartPolicy: Never
      volumes:
        - name: kubeconfig
          secret:
            secretName: "{{ .ObjectMeta.Name }}-kubeconfig"
