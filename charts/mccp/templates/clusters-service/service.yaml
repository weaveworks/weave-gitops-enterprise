---
apiVersion: v1
kind: Service
metadata:
  name: clusters-service
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: clusters-service
  type: ClusterIP
